用户评论情感分析系统
基于大语言模型的用户评论情感分析工具，从原始评论数据中自动提取情感倾向、关键原因与高频词，并生成可视化报告。
本项目由 Claude（claude-sonnet4.6）辅助编程完成，完整记录了从需求定义、模块设计到代码调试的全过程。

 核心功能
1. 评论真实性过滤
自动识别并剔除模板化、空洞的刷评/水军内容，只保留有效评论用于分析。评分低于阈值（默认5/10）的评论会被标记并单独列出。
2. 批量情感分析
将评论分批（默认每批 10 条）合并进单次 API 调用，相比逐条调用节省约 80% 的 API 消耗和等待时间。每条评论输出：
  情感标签： positive / neutral / negative
  情感原因：一句话说明
  关键词：3-5 个具体描述词（自动过滤纯情感词）
3. 差评原因聚类
汇总所有差评，提炼出 3-5 个核心问题，并统计各原因对应的评论数量，生成可量化的差评原因分布图。
4. 竞品对比分析（可选）
同时输入两个产品的评论，对比好评率、差评率、高频关键词，并从产品经理视角输出差距分析与优先改进方向。
5. Actionable 改进建议
基于差评原因和竞品差距，生成针对前 3 大问题的具体产品改进建议，格式：问题描述 → 建议动作 → 预期效果。

6. 可视化& 报告输出
  chart_sentiment_pie.png ：情感分布环形图
  chart_neg_reasons.png ：差评原因横向条形图
  chart_competitor.png ：竞品对比图（有竞品数据时生成）
  result.docx ：完整分析报告，包含数据质量说明、情感总结、图表、逐条明细



安装依赖

pip install -r requirements.txt


配置 API Key
在项目根目录创建  .env 文件（不要上传到 GitHub）：

DEEPSEEK_API_KEY=your_api_key_here


准备数据
将导出的 JSON 文件放入  data/ 文件夹。JSON 结构需满足：

{
"result": { "items": [
{ "content": "评论正文 " } ]
}
}

没有真实数据？可以先用  data/sample_reviews.json 中的示例数据跑通流程。


运行

python main.py

 项目结构

.
├── main.py                   # 主流程
├── sentiment_batch.py       # 批量情感分析   & 真实性过滤模块
├── visualize.py             # 可视化模块
├── report.py                # docx 报告生成模块
├── data/
|   L── sample_reviews.json  # 示例数据（虚构，可直接运行） ├── requirements.txt
L── README.md




关键参数说明
参数	默认值	说明
batch_size	10	每批分析的评论数量，建议 8-10
authenticity_threshold	5	真实性分数低于此值视为可疑
competitor_file	None	竞品评论路径，填写后自动开启竞品对比


技术说明
为什么用批量调用？
逐条调用时， 100 条评论需要 100 次 API 请求。批量模式将 10 条合并为 1 次调用， 速度提升约8 倍 ，token 消耗也显著降低。同时实现了长度校验机制 ：若模型返回条数不符，自动逐条
fallback 重试。
Prompt 设计思路
情感分析 prompt 强制要求输出 JSON，通过 few-shot 关键词规则约束输出质量（禁止纯情感词、禁止语义重复）， 减少后处理成本。真实性过滤 prompt 则基于反刷评领域的判断维度设计评分标准。
编程方式
本项目通过与 Claude 对话完成，包括模块拆分设计、批量调用改造 、错误处理逻辑、可视化样式调整等 。Claude 生成初版代码后，根据实际运行结果迭代修改。


 注意事项
 本项目仅用于学习与个人作品展示
 请勿将真实用户评论数据上传公开
 API Key 请通过 .env 管理，.env 已加入.gitignore